import { NgModule } from '@angular/core';
import { ArrayGenerationModule } from '../ArrayGeneration/ArrayGenerationModule';

@NgModule({
    imports: [
        ArrayGenerationModule
    ]
})
export class WordSearchStateModule {
}